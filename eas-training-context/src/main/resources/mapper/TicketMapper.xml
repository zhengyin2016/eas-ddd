<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eas.trainingcontext.southbound.port.repository.TicketMapperPort">

    <select id="findById" resultType="com.eas.trainingcontext.domain.ticket.entity.Ticket">
        SELECT id, training_id, status
        FROM training_ticket
        WHERE id = #{id}
    </select>

    <select id="findByTrainingIdAndStatus" resultType="com.eas.trainingcontext.domain.ticket.entity.Ticket">
        SELECT id, training_id, status
        FROM training_ticket
        WHERE training_id = #{trainingId} AND status = #{status}
        LIMIT 1
    </select>

    <insert id="save" parameterType="com.eas.trainingcontext.domain.ticket.entity.Ticket">
        INSERT INTO training_ticket (id, training_id, status)
        VALUES (#{id}, #{trainingId}, #{status})
        ON DUPLICATE KEY UPDATE status = #{status}
    </insert>

</mapper>
